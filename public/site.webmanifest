{
  "name": "Skim — a clean URL-to-markdown reader for agents",
  "short_name": "Skim",
  "description": "Skim is a clean URL-to-markdown reader for agents. Card API key (sk402_) is the default path; USDC on Base via x402 is optional.",
  "start_url": "/",
  "scope": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#236CFF",
  "icons": [
    {
      "src": "/favicon.svg",
      "type": "image/svg+xml",
      "sizes": "any",
      "purpose": "any maskable"
    }
  ]
}
